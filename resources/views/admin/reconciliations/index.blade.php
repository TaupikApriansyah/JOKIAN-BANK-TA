@extends('layouts.app', ['pageTitle' => 'Rekonsiliasi Kas Harian'])

@section('content')
<div class="mb-6 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><h1 class="text-2xl font-bold">Rekonsiliasi Kas Harian</h1><p class="mt-1 text-sm text-slate-500">Bandingkan penerimaan transaksi yang disetujui dengan kas fisik atau bukti setoran.</p></div><form class="flex gap-2"><input name="date" type="date" value="{{ $date }}" class="rounded-lg border-slate-300 text-sm"><button class="rounded-lg bg-brand-800 px-4 py-2 text-sm font-semibold text-white">Tampilkan</button></form></div>
<div class="grid gap-4 md:grid-cols-3"><div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-xs font-bold uppercase text-slate-500">Penerimaan Sistem</p><p class="mt-2 text-2xl font-bold">Rp{{ number_format($systemTotal, 0, ',', '.') }}</p></div><div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-xs font-bold uppercase text-slate-500">Kas Fisik / Setoran</p><p class="mt-2 text-2xl font-bold">{{ $reconciliation ? 'Rp'.number_format($reconciliation->physical_total, 0, ',', '.') : 'Belum diinput' }}</p></div><div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-xs font-bold uppercase text-slate-500">Status</p><p class="mt-2 text-lg font-bold {{ $reconciliation?->status === 'sesuai' ? 'text-emerald-600' : 'text-yellow-600' }}">{{ $reconciliation ? str($reconciliation->status)->replace('_', ' ')->title() : 'Belum Direkonsiliasi' }}</p></div></div>
<div class="mt-6 grid gap-6 lg:grid-cols-3"><section class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm lg:col-span-2"><h2 class="font-semibold">Input Rekonsiliasi</h2><form method="POST" action="{{ route('admin.reconciliations.store') }}" class="mt-5 grid gap-4 md:grid-cols-2">@csrf<input type="hidden" name="reconciliation_date" value="{{ $date }}"><label class="text-sm font-medium">Tanggal Rekonsiliasi<input disabled value="{{ $date }}" class="mt-1.5 w-full rounded-lg border-slate-200 bg-slate-50"></label><label class="text-sm font-medium">Kas Fisik / Bukti Setoran (Rp)<input name="physical_total" type="number" min="0" value="{{ old('physical_total', $reconciliation?->physical_total) }}" required class="mt-1.5 w-full rounded-lg border-slate-300"></label><label class="text-sm font-medium md:col-span-2">Catatan Admin<textarea name="note" rows="3" class="mt-1.5 w-full rounded-lg border-slate-300" placeholder="Wajib dijelaskan bila terjadi selisih.">{{ old('note', $reconciliation?->note) }}</textarea></label><div class="flex justify-end md:col-span-2"><button class="rounded-lg bg-brand-700 px-4 py-2 text-sm font-semibold text-white">Simpan Rekonsiliasi</button></div></form></section><section class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-semibold">Riwayat Terbaru</h2><div class="mt-4 space-y-3">@forelse($recentReconciliations as $item)<div class="border-b pb-3 text-sm last:border-0"><p class="font-semibold">{{ $item->reconciliation_date->format('d M Y') }}</p><p class="mt-1 text-slate-600">Selisih: Rp{{ number_format($item->difference, 0, ',', '.') }}</p><p class="text-xs text-slate-400">{{ $item->createdBy->name }}</p></div>@empty<p class="text-sm text-slate-500">Belum ada riwayat.</p>@endforelse</div></section></div>
@endsection
