@extends('layouts.app', ['pageTitle' => 'Koreksi Transaksi'])

@section('content')
<div class="mb-6"><h1 class="text-2xl font-bold">Verifikasi Koreksi Transaksi</h1><p class="mt-1 text-sm text-slate-500">Koreksi yang disetujui akan membuat jurnal pembalik dan draft transaksi pengganti.</p></div>
<div class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Transaksi</th><th class="px-5 py-3">Nasabah</th><th class="px-5 py-3">Maker</th><th class="px-5 py-3">Nominal Lama</th><th class="px-5 py-3">Nominal Usulan</th><th class="px-5 py-3"></th></tr></thead><tbody class="divide-y">@forelse($corrections as $correction)<tr><td class="px-5 py-3 font-mono text-xs">{{ $correction->transaction->transaction_number }}</td><td class="px-5 py-3">{{ $correction->transaction->customer->name }}</td><td class="px-5 py-3">{{ $correction->requestedBy->name }}</td><td class="px-5 py-3">Rp{{ number_format($correction->transaction->amount, 0, ',', '.') }}</td><td class="px-5 py-3 font-semibold">Rp{{ number_format($correction->proposed_amount, 0, ',', '.') }}</td><td class="px-5 py-3 text-right"><a class="rounded-lg bg-brand-800 px-3 py-2 text-xs font-semibold text-white" href="{{ route('admin.corrections.show', $correction) }}">Periksa</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-500">Tidak ada koreksi yang menunggu verifikasi.</td></tr>@endforelse</tbody></table></div></div><div class="mt-5">{{ $corrections->links() }}</div>
@endsection
