@extends('layouts.app', ['pageTitle' => 'Detail Koreksi Transaksi'])

@section('content')
<div class="mb-6"><p class="font-mono text-xs font-semibold text-brand-700">{{ $correction->transaction->transaction_number }}</p><h1 class="text-2xl font-bold">Verifikasi Koreksi Transaksi</h1><p class="mt-1 text-sm text-slate-500">Pastikan alasan koreksi dan nilai pengganti sesuai sebelum menyetujui.</p></div>
<div class="grid gap-6 lg:grid-cols-3"><section class="space-y-6 lg:col-span-2"><div class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold">Perbandingan Nilai</h2><div class="mt-5 grid gap-4 md:grid-cols-2"><div class="rounded-lg bg-slate-50 p-4"><p class="text-xs font-bold uppercase text-slate-500">Transaksi Lama</p><p class="mt-2 font-semibold">{{ $correction->transaction->category }}</p><p class="mt-1 text-xl font-bold">Rp{{ number_format($correction->transaction->amount, 0, ',', '.') }}</p></div><div class="rounded-lg bg-yellow-50 p-4"><p class="text-xs font-bold uppercase text-yellow-700">Usulan Pengganti</p><p class="mt-2 font-semibold">{{ $correction->proposed_category }}</p><p class="mt-1 text-xl font-bold">Rp{{ number_format($correction->proposed_amount, 0, ',', '.') }}</p></div></div><div class="mt-5"><p class="text-xs font-bold uppercase text-slate-500">Alasan Koreksi</p><p class="mt-2 text-sm text-slate-700">{{ $correction->reason }}</p></div><div class="mt-5"><p class="text-xs font-bold uppercase text-slate-500">Keterangan Baru</p><p class="mt-2 text-sm text-slate-700">{{ $correction->proposed_description ?: '-' }}</p></div></div><div class="rounded-xl bg-slate-950 p-5 font-mono text-xs text-slate-200"><p class="mb-3 text-slate-400">Preview Jurnal Pembalik Otomatis</p><div class="flex justify-between"><span>Dr. {{ $correction->transaction->credit_account }}</span><span>Rp{{ number_format($correction->transaction->amount, 0, ',', '.') }}</span></div><div class="mt-2 flex justify-between"><span>Cr. {{ $correction->transaction->debit_account }}</span><span>Rp{{ number_format($correction->transaction->amount, 0, ',', '.') }}</span></div></div></section><aside class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-semibold">Keputusan Checker</h2>@if($correction->status->value === 'menunggu_verifikasi')<form method="POST" action="{{ route('admin.corrections.verify', $correction) }}" class="mt-4 space-y-4">@csrf<label class="block text-sm font-medium">Catatan Checker<textarea name="note" rows="4" class="mt-1.5 w-full rounded-lg border-slate-300" placeholder="Wajib untuk menolak."></textarea></label><button name="decision" value="approve" class="w-full rounded-lg bg-brand-700 px-4 py-2.5 text-sm font-semibold text-white">Setujui Koreksi</button><button name="decision" value="reject" class="w-full rounded-lg border border-red-200 bg-red-50 px-4 py-2.5 text-sm font-semibold text-red-700">Tolak Koreksi</button></form>@else<div class="mt-4 rounded-lg bg-slate-50 p-3 text-sm">Status: <strong>{{ $correction->status->label() }}</strong><p class="mt-2 text-slate-500">{{ $correction->review_note }}</p></div>@endif</aside></div>
@endsection
