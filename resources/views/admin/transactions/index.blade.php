@extends('layouts.app', ['pageTitle' => 'Verifikasi Transaksi'])
@section('content')
<div class="mb-6"><h1 class="text-2xl font-bold">Verifikasi Transaksi</h1><p class="mt-1 text-sm text-slate-500">Maker–Checker: transaksi tidak dapat diposting sebelum disetujui oleh Admin.</p></div><div class="rounded-xl border border-slate-200 bg-white shadow-sm overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Waktu</th><th class="px-5 py-3">No. Transaksi</th><th class="px-5 py-3">Maker</th><th class="px-5 py-3">Berkas</th><th class="px-5 py-3">Kategori</th><th class="px-5 py-3">Nominal</th><th class="px-5 py-3"></th></tr></thead><tbody class="divide-y">@forelse($transactions as $transaction)<tr><td class="px-5 py-3 text-slate-500">{{ $transaction->submitted_at?->format('d/m/Y H:i') }}</td><td class="px-5 py-3 font-mono text-xs font-semibold">{{ $transaction->transaction_number }}</td><td class="px-5 py-3">{{ $transaction->createdBy->name }}</td><td class="px-5 py-3 font-mono text-xs">{{ $transaction->serviceCase->file_number }}</td><td class="px-5 py-3">{{ $transaction->category }}</td><td class="px-5 py-3 font-bold">Rp{{ number_format($transaction->amount,0,',','.') }}</td><td class="px-5 py-3 text-right"><a class="rounded-lg bg-brand-800 px-3 py-1.5 text-xs font-semibold text-white" href="{{ route('admin.transactions.show',$transaction) }}">Periksa</a></td></tr>@empty<tr><td colspan="7" class="p-8 text-center text-slate-500">Tidak ada transaksi yang menunggu verifikasi.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $transactions->links() }}</div>
@endsection
