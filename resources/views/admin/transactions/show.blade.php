@extends('layouts.app', ['pageTitle' => 'Detail Verifikasi'])
@section('content')
<div class="flex justify-between gap-4 mb-6"><div><p class="font-mono text-xs font-semibold text-brand-700">{{ $transaction->transaction_number }}</p><h1 class="text-2xl font-bold">Verifikasi Transaksi</h1><p class="mt-1 text-sm text-slate-500">Pastikan nominal, kategori, bukti, dan referensi berkas sesuai sebelum menyetujui.</p></div>@include('partials.transaction-badge',['transaction'=>$transaction])</div>
<div class="grid lg:grid-cols-3 gap-6"><section class="lg:col-span-2 rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold">Detail Pengajuan Maker</h2><dl class="mt-5 grid sm:grid-cols-2 gap-5 text-sm"><div><dt class="text-slate-500">Nasabah</dt><dd class="font-medium">{{ $transaction->customer->name }}</dd></div><div><dt class="text-slate-500">Berkas Referensi</dt><dd class="font-mono text-xs font-semibold">{{ $transaction->serviceCase->file_number }}</dd></div><div><dt class="text-slate-500">Maker</dt><dd class="font-medium">{{ $transaction->createdBy->name }}</dd></div><div><dt class="text-slate-500">Kategori</dt><dd class="font-medium">{{ $transaction->category }}</dd></div><div><dt class="text-slate-500">Metode Pembayaran</dt><dd class="font-medium">{{ $transaction->payment_method }}</dd></div><div><dt class="text-slate-500">Nominal</dt><dd class="text-lg font-bold">Rp{{ number_format($transaction->amount,0,',','.') }}</dd></div><div class="sm:col-span-2"><dt class="text-slate-500">Keterangan</dt><dd class="font-medium">{{ $transaction->description ?: '-' }}</dd></div></dl><div class="mt-6 rounded-lg bg-slate-950 p-4 font-mono text-xs text-slate-200"><p class="mb-2 text-slate-400">Preview Jurnal Sistem</p><div class="flex justify-between"><span>Dr. {{ $transaction->debit_account }}</span><span>Rp{{ number_format($transaction->amount,0,',','.') }}</span></div><div class="mt-2 flex justify-between"><span>Cr. {{ $transaction->credit_account }}</span><span>Rp{{ number_format($transaction->amount,0,',','.') }}</span></div></div></section><aside class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-semibold">Keputusan Checker</h2>@if($transaction->status->value === 'menunggu_verifikasi')<form method="POST" action="{{ route('admin.transactions.verify',$transaction) }}" class="mt-4 space-y-4">@csrf<label class="block text-sm font-medium">Catatan Verifikasi<textarea name="note" rows="4" class="mt-1.5 w-full rounded-lg border-slate-300 text-sm" placeholder="Wajib diisi bila dikembalikan."></textarea></label><button name="decision" value="approve" class="w-full rounded-lg bg-brand-700 px-4 py-2.5 text-sm font-semibold text-white">✓ Setujui & Buat Jurnal</button><button name="decision" value="return" class="w-full rounded-lg border border-red-200 bg-red-50 px-4 py-2.5 text-sm font-semibold text-red-700">↩ Kembalikan untuk Perbaikan</button></form>@else<div class="mt-4 rounded-lg bg-slate-50 p-3 text-sm"><p>Status akhir: <strong>{{ $transaction->status->label() }}</strong></p><p class="mt-2 text-slate-500">{{ $transaction->verification_note }}</p></div>@endif</aside></div>
@endsection
