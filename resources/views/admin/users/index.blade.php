@extends('layouts.app', ['pageTitle' => 'Manajemen User'])
@section('content')
<div class="mb-6"><h1 class="text-2xl font-extrabold">Manajemen User</h1><p class="mt-1 text-sm text-slate-500">Admin membuat, membaca, mengubah, dan menonaktifkan akun. Penghapusan fisik tidak digunakan agar audit trail tetap utuh.</p></div><div class="grid gap-6 xl:grid-cols-3"><section class="app-card p-6"><h2 class="font-extrabold">Tambah Pengguna</h2><form method="POST" action="{{ route('admin.users.store') }}" class="mt-5 space-y-4" data-processing-overlay>@csrf<label class="block text-sm font-bold">ID Pegawai<input name="employee_id" value="{{ old('employee_id') }}" required class="form-input mt-1.5"></label><label class="block text-sm font-bold">Nama<input name="name" value="{{ old('name') }}" required class="form-input mt-1.5"></label><label class="block text-sm font-bold">Email<input name="email" type="email" value="{{ old('email') }}" required class="form-input mt-1.5"></label><label class="block text-sm font-bold">Role<select name="role" class="form-input mt-1.5"><option value="cs">Customer Service / Maker</option><option value="admin">Admin Supervisor / Checker</option></select></label><label class="block text-sm font-bold">Password Awal<input name="password" type="password" required class="form-input mt-1.5"></label><label class="block text-sm font-bold">Konfirmasi Password<input name="password_confirmation" type="password" required class="form-input mt-1.5"></label><button class="animated-button w-full"><span>Buat Akun</span><span></span></button></form></section><section class="table-shell xl:col-span-2"><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs font-bold uppercase text-slate-500"><tr><th class="px-5 py-3">ID / Nama</th><th class="px-5 py-3">Role</th><th class="px-5 py-3">Login Terakhir</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($users as $user)<tr><td class="px-5 py-4"><p class="font-bold">{{ $user->name }}</p><p class="text-xs text-slate-500">{{ $user->employee_id }} · {{ $user->email }}</p></td><td class="px-5 py-4">{{ $user->role->label() }}</td><td class="px-5 py-4 text-xs">{{ $user->last_login_at?->format('d M Y H:i') ?? 'Belum pernah' }}</td><td class="px-5 py-4"><span class="status-chip {{ $user->is_active?'bg-emerald-100 text-emerald-700':'bg-slate-200 text-slate-600' }}">{{ $user->is_active?'Aktif':'Nonaktif' }}</span></td><td class="px-5 py-4 text-right"><div class="inline-flex items-center gap-2"><a class="soft-button !py-2 !text-xs" href="{{ route('admin.users.edit',$user) }}">Ubah</a>@if($user->id!==auth()->id())<form method="POST" action="{{ route('admin.users.status',$user) }}" onsubmit="return confirm('{{ $user->is_active?'Nonaktifkan':'Aktifkan' }} akun ini?');" data-processing-overlay>@csrf @method('PUT')<input type="hidden" name="is_active" value="{{ $user->is_active?0:1 }}">@if($user->is_active)<x-destructive-button label="Nonaktifkan"/>@else<button class="soft-button !py-2 !text-xs">Aktifkan</button>@endif</form>@endif</div></td></tr>@endforeach</tbody></table></div></section></div><div class="mt-5">{{ $users->links() }}</div>
@endsection
