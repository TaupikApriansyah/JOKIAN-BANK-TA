@extends('layouts.app', ['pageTitle' => 'Audit Trail'])
@section('content')
<div class="mb-6"><h1 class="text-2xl font-bold">Audit Trail</h1><p class="mt-1 text-sm text-slate-500">Log bersifat append-only untuk mendukung penelusuran aktivitas dan akuntabilitas.</p></div><form class="mb-5 grid md:grid-cols-3 gap-3"><input name="module" value="{{ request('module') }}" class="rounded-lg border-slate-300 text-sm" placeholder="Filter modul, mis. transaction"><input name="action" value="{{ request('action') }}" class="rounded-lg border-slate-300 text-sm" placeholder="Filter aksi, mis. approve"><button class="rounded-lg border border-slate-300 bg-white text-sm font-semibold">Terapkan</button></form><div class="rounded-xl border border-slate-200 bg-white shadow-sm overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Waktu</th><th class="px-5 py-3">Pengguna</th><th class="px-5 py-3">Modul / Aksi</th><th class="px-5 py-3">Objek</th><th class="px-5 py-3">IP</th><th class="px-5 py-3">Keterangan</th></tr></thead><tbody class="divide-y">@forelse($logs as $log)<tr><td class="px-5 py-3 text-xs">{{ $log->created_at->format('d/m/Y H:i:s') }}</td><td class="px-5 py-3">{{ $log->user?->name ?? 'Sistem' }}<p class="text-xs text-slate-400">{{ $log->role }}</p></td><td class="px-5 py-3"><span class="font-semibold">{{ $log->module }}</span><p class="text-xs text-slate-500">{{ $log->action }}</p></td><td class="px-5 py-3 text-xs">{{ class_basename($log->subject_type) }} #{{ $log->subject_id }}</td><td class="px-5 py-3 text-xs">{{ $log->ip_address }}</td><td class="px-5 py-3">{{ $log->description }}</td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-slate-500">Belum ada audit log.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $logs->links() }}</div>
@endsection
