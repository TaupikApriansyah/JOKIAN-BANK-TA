@extends('layouts.app', ['pageTitle' => 'Detail Jurnal'])
@section('content')
<div class="mb-6 flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p class="font-mono text-xs font-bold text-brand-700">{{ $journal->journal_number }}</p><h1 class="mt-1 text-2xl font-extrabold">Tinjau Jurnal</h1><p class="mt-1 text-sm text-slate-500">Pastikan transaksi, akun debit-kredit, dan nominal sudah benar sebelum posting.</p></div><span class="status-chip {{ $journal->status->value === 'posted' ? 'bg-emerald-100 text-emerald-700' : 'bg-yellow-100 text-yellow-700' }}">{{ $journal->status->label() }}</span></div>
<div class="grid gap-6 lg:grid-cols-3"><section class="app-card p-6 lg:col-span-2"><h2 class="font-extrabold">Informasi Jurnal</h2><dl class="mt-5 grid gap-5 md:grid-cols-2"><div><dt class="text-xs font-bold uppercase text-slate-400">Nomor Transaksi</dt><dd class="mt-1 font-semibold">{{ $journal->transaction->transaction_number }}</dd></div><div><dt class="text-xs font-bold uppercase text-slate-400">Nasabah</dt><dd class="mt-1 font-semibold">{{ $journal->transaction->customer->name }}</dd></div><div><dt class="text-xs font-bold uppercase text-slate-400">Debit</dt><dd class="mt-1 rounded-lg bg-slate-50 p-3 font-mono text-sm">{{ $journal->debit_account }}</dd></div><div><dt class="text-xs font-bold uppercase text-slate-400">Kredit</dt><dd class="mt-1 rounded-lg bg-slate-50 p-3 font-mono text-sm">{{ $journal->credit_account }}</dd></div><div><dt class="text-xs font-bold uppercase text-slate-400">Nominal</dt><dd class="mt-1 text-2xl font-extrabold">Rp{{ number_format($journal->amount,0,',','.') }}</dd></div><div><dt class="text-xs font-bold uppercase text-slate-400">Tipe</dt><dd class="mt-1 font-semibold">{{ $journal->entry_type === 'reversal' ? 'Jurnal Pembalik' : 'Jurnal Normal' }}</dd></div></dl><div class="mt-6 border-t border-slate-100 pt-5"><h3 class="font-bold">Jejak Persetujuan</h3><div class="mt-3 grid gap-3 text-sm md:grid-cols-2"><p><span class="text-slate-400">Maker:</span> {{ $journal->transaction->createdBy->name }}</p><p><span class="text-slate-400">Checker:</span> {{ $journal->preparedBy?->name ?? $journal->transaction->verifiedBy?->name ?? '-' }}</p><p><span class="text-slate-400">Akuntan:</span> {{ $journal->postedBy?->name ?? '-' }}</p><p><span class="text-slate-400">Waktu posting:</span> {{ $journal->posted_at?->format('d M Y H:i') ?? '-' }}</p></div>@if($journal->review_note)<p class="mt-4 rounded-xl bg-blue-50 p-4 text-sm text-blue-800"><b>Catatan Akuntan:</b> {{ $journal->review_note }}</p>@endif</div></section>
<aside class="app-card p-6"><h2 class="font-extrabold">Aksi Akuntan</h2>@if($journal->status->value === 'draft')<form method="POST" action="{{ route('accountant.journals.post',$journal) }}" class="mt-5 space-y-4" onsubmit="return confirm('Posting jurnal ini? Jurnal yang sudah diposting tidak dapat diedit langsung.');" data-processing-overlay>@csrf<label class="block text-sm font-bold">Catatan Pemeriksaan<textarea name="review_note" rows="4" class="form-input mt-1.5" placeholder="Opsional, tuliskan hasil pemeriksaan.">{{ old('review_note') }}</textarea></label><button class="animated-button w-full"><span>Posting Jurnal</span><span></span><i data-lucide="badge-check" class="h-4 w-4"></i></button></form>@else<div class="mt-5 rounded-xl border border-emerald-100 bg-emerald-50 p-4 text-sm text-emerald-800"><p class="font-extrabold">Jurnal sudah final</p><p class="mt-1">Perubahan harus dilakukan melalui mekanisme koreksi dan jurnal pembalik.</p></div>@endif<a href="{{ route('accountant.journals.index') }}" class="soft-button mt-4 w-full justify-center">Kembali</a></aside></div>
@endsection
