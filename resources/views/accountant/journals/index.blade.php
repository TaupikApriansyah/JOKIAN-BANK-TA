@extends('layouts.app', ['pageTitle' => 'Jurnal Akuntansi'])
@section('content')
<div class="mb-6 flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><h1 class="text-2xl font-extrabold">Jurnal Akuntansi</h1><p class="mt-1 text-sm text-slate-500">Draft jurnal dibuat setelah transaksi disetujui Checker dan hanya dapat diposting oleh Akuntan.</p></div><div class="flex gap-3"><span class="status-chip bg-yellow-100 text-yellow-700">{{ $pendingCount }} menunggu</span><span class="status-chip bg-emerald-100 text-emerald-700">{{ $postedCount }} diposting</span></div></div>
<form method="GET" class="app-card mb-6 grid gap-3 p-4 md:grid-cols-5"><input name="search" value="{{ request('search') }}" class="form-input" placeholder="Nomor jurnal/transaksi/nasabah"><select name="status" class="form-input"><option value="">Semua status</option><option value="draft" @selected(request('status')==='draft')>Menunggu Posting</option><option value="posted" @selected(request('status')==='posted')>Sudah Diposting</option></select><select name="entry_type" class="form-input"><option value="">Semua tipe</option><option value="normal" @selected(request('entry_type')==='normal')>Normal</option><option value="reversal" @selected(request('entry_type')==='reversal')>Pembalik</option></select><input type="date" name="date_from" value="{{ request('date_from') }}" class="form-input"><div class="flex gap-2"><input type="date" name="date_to" value="{{ request('date_to') }}" class="form-input min-w-0"><button class="animated-button !px-4"><span>Filter</span><span></span></button></div></form>
<section class="table-shell"><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs font-bold uppercase text-slate-500"><tr><th class="px-5 py-3">Nomor</th><th class="px-5 py-3">Transaksi</th><th class="px-5 py-3">Akun</th><th class="px-5 py-3">Nominal</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($journals as $journal)<tr><td class="px-5 py-4"><p class="font-mono font-bold">{{ $journal->journal_number }}</p><p class="text-xs text-slate-400">{{ $journal->entry_type === 'reversal' ? 'Pembalik' : 'Normal' }}</p></td><td class="px-5 py-4"><p class="font-semibold">{{ $journal->transaction->transaction_number }}</p><p class="text-xs text-slate-500">{{ $journal->transaction->customer->name }}</p></td><td class="px-5 py-4 text-xs"><p><b>D:</b> {{ $journal->debit_account }}</p><p class="mt-1"><b>K:</b> {{ $journal->credit_account }}</p></td><td class="px-5 py-4 font-bold">Rp{{ number_format($journal->amount,0,',','.') }}</td><td class="px-5 py-4"><span class="status-chip {{ $journal->status->value === 'posted' ? 'bg-emerald-100 text-emerald-700' : 'bg-yellow-100 text-yellow-700' }}">{{ $journal->status->label() }}</span></td><td class="px-5 py-4 text-right"><a href="{{ route('accountant.journals.show',$journal) }}" class="soft-button !py-2 !text-xs">Detail</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-400">Data jurnal tidak ditemukan.</td></tr>@endforelse</tbody></table></div></section><div class="mt-5">{{ $journals->links() }}</div>
@endsection
