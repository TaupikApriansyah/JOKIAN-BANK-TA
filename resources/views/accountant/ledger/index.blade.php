@extends('layouts.app', ['pageTitle' => 'Buku Besar'])
@section('content')
<div class="mb-6"><h1 class="text-2xl font-extrabold">Buku Besar</h1><p class="mt-1 text-sm text-slate-500">Mutasi debit dan kredit berdasarkan jurnal yang telah diposting.</p></div>
<form method="GET" class="app-card mb-6 grid gap-3 p-4 md:grid-cols-4"><select name="account" class="form-input md:col-span-2"><option value="">Pilih akun</option>@foreach($accounts as $account)<option value="{{ $account }}" @selected($selectedAccount===$account)>{{ $account }}</option>@endforeach</select><input type="date" name="date_from" value="{{ request('date_from') }}" class="form-input"><div class="flex gap-2"><input type="date" name="date_to" value="{{ request('date_to') }}" class="form-input min-w-0"><button class="animated-button !px-4"><span>Filter</span><span></span></button></div></form>
@if($selectedAccount)<div class="mb-5 grid gap-4 md:grid-cols-3"><div class="app-card p-5"><p class="text-xs font-bold uppercase text-slate-500">Total Debit</p><p class="mt-2 text-2xl font-extrabold">Rp{{ number_format($totalDebit,0,',','.') }}</p></div><div class="app-card p-5"><p class="text-xs font-bold uppercase text-slate-500">Total Kredit</p><p class="mt-2 text-2xl font-extrabold">Rp{{ number_format($totalCredit,0,',','.') }}</p></div><div class="app-card p-5"><p class="text-xs font-bold uppercase text-slate-500">Saldo Debit - Kredit</p><p class="mt-2 text-2xl font-extrabold {{ $balance < 0 ? 'text-red-600' : 'text-slate-900' }}">Rp{{ number_format($balance,0,',','.') }}</p></div></div>
<section class="table-shell"><div class="border-b border-slate-100 px-5 py-4"><h2 class="font-extrabold">{{ $selectedAccount }}</h2></div><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs font-bold uppercase text-slate-500"><tr><th class="px-5 py-3">Tanggal</th><th class="px-5 py-3">Jurnal / Transaksi</th><th class="px-5 py-3">Nasabah</th><th class="px-5 py-3 text-right">Debit</th><th class="px-5 py-3 text-right">Kredit</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($entries as $entry)<tr><td class="px-5 py-4">{{ $entry->posted_at?->format('d M Y') }}</td><td class="px-5 py-4"><a class="font-mono font-bold text-brand-700" href="{{ route('accountant.journals.show',$entry) }}">{{ $entry->journal_number }}</a><p class="text-xs text-slate-400">{{ $entry->transaction->transaction_number }}</p></td><td class="px-5 py-4">{{ $entry->transaction->customer->name }}</td><td class="px-5 py-4 text-right font-semibold">{{ $entry->debit_account === $selectedAccount ? 'Rp'.number_format($entry->amount,0,',','.') : '-' }}</td><td class="px-5 py-4 text-right font-semibold">{{ $entry->credit_account === $selectedAccount ? 'Rp'.number_format($entry->amount,0,',','.') : '-' }}</td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-slate-400">Belum ada mutasi pada akun ini.</td></tr>@endforelse</tbody></table></div></section>@else<div class="app-card p-10 text-center text-slate-500">Belum ada akun dari jurnal yang sudah diposting.</div>@endif
@endsection
