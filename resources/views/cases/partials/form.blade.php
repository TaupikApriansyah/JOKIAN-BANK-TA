<div class="grid gap-5 md:grid-cols-2"><label class="text-sm font-semibold">Nasabah<select name="customer_id" class="form-input mt-1.5" required>@foreach($customers as $customer)<option value="{{ $customer->id }}" @selected(old('customer_id',$serviceCase?->customer_id)===$customer->id)>{{ $customer->name }} · {{ $customer->customer_number }}</option>@endforeach</select></label><label class="text-sm font-semibold">Jenis Layanan<select name="service_type_id" class="form-input mt-1.5" required>@foreach($serviceTypes as $type)<option value="{{ $type->id }}" @selected(old('service_type_id',$serviceCase?->service_type_id)===$type->id)>{{ $type->name }} · SLA {{ $type->sla_hours }} jam</option>@endforeach</select></label><label class="text-sm font-semibold md:col-span-2">Tanggal & Jam Berkas Masuk<input name="received_at" type="datetime-local" class="form-input mt-1.5" value="{{ old('received_at',$serviceCase?->received_at?->format('Y-m-d\TH:i') ?? now()->format('Y-m-d\TH:i')) }}" required></label><label class="text-sm font-semibold md:col-span-2">Catatan Layanan<textarea name="notes" rows="4" class="form-input mt-1.5">{{ old('notes',$serviceCase?->notes) }}</textarea></label></div>
