@extends('layouts.app', ['pageTitle' => 'Berkas Nasabah'])
@section('content')
<div class="mb-6 flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><h1 class="text-2xl font-extrabold">Berkas Nasabah</h1><p class="mt-1 text-sm text-slate-500">Pantau kelengkapan dokumen, proses layanan, dan batas SLA.</p></div>@if(auth()->user()->isCustomerService())<a href="{{ route('cases.create') }}" class="primary-pill"><i data-lucide="folder-plus" class="h-4 w-4"></i> Input Berkas</a>@endif</div>
<form class="mb-5 grid gap-3 lg:grid-cols-4"><input name="q" value="{{ request('q') }}" class="form-input" placeholder="Cari nomor berkas atau nasabah"><select name="status" class="form-input"><option value="">Semua Status Berkas</option>@foreach(['baru'=>'Baru','menunggu_dokumen'=>'Menunggu Dokumen','diproses'=>'Diproses','selesai'=>'Selesai','ditolak'=>'Ditolak'] as $value=>$label)<option value="{{ $value }}" @selected(request('status')===$value)>{{ $label }}</option>@endforeach</select><select name="sla_status" class="form-input"><option value="">Semua Status SLA</option>@foreach(['aman'=>'Aman','mendekati'=>'Mendekati SLA','terlambat'=>'Terlambat','selesai'=>'Selesai'] as $value=>$label)<option value="{{ $value }}" @selected(request('sla_status')===$value)>{{ $label }}</option>@endforeach</select><button class="soft-button justify-center"><i data-lucide="sliders-horizontal" class="h-4 w-4"></i> Terapkan</button></form>
<div class="table-shell overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-blue-50/70 text-left text-xs font-bold uppercase text-slate-500"><tr><th class="px-5 py-3">No. Berkas</th><th class="px-5 py-3">Nasabah</th><th class="px-5 py-3">Layanan</th><th class="px-5 py-3">Batas SLA</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-blue-50">@forelse($cases as $case)<tr class="hover:bg-blue-50/30"><td class="px-5 py-4 font-mono text-xs font-bold">{{ $case->file_number }}</td><td class="px-5 py-4"><p class="font-semibold">{{ $case->customer->name }}</p><p class="text-xs text-slate-400">{{ $case->assignedTo->name }}</p></td><td class="px-5 py-4">{{ $case->serviceType->name }}</td><td class="px-5 py-4"><p class="text-xs">{{ $case->due_at->format('d M Y H:i') }}</p><div class="mt-1">@include('partials.sla-badge',['serviceCase'=>$case])</div></td><td class="px-5 py-4"><span class="status-chip bg-blue-100 text-blue-700">{{ $case->status->label() }}</span></td><td class="px-5 py-4 text-right"><a href="{{ route('cases.show',$case) }}" class="text-xs font-bold text-brand-700">Workspace</a>@if(auth()->user()->isCustomerService() && $case->assigned_to===auth()->id() && in_array($case->status->value,['baru','menunggu_dokumen']))<a href="{{ route('cases.edit',$case) }}" class="ml-3 text-xs font-bold text-slate-600">Ubah</a>@endif</td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-500">Tidak ada berkas.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $cases->links() }}</div>
@endsection
