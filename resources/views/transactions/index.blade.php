@extends('layouts.app', ['pageTitle' => 'Transaksi Administrasi'])
@section('content')
<div class="mb-6 flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><h1 class="text-2xl font-extrabold">Transaksi Administrasi</h1><p class="mt-1 text-sm text-slate-500">{{ auth()->user()->isAdmin() ? 'Riwayat seluruh transaksi yang tersedia untuk monitoring.' : 'Riwayat transaksi yang Anda buat sebagai Maker.' }}</p></div><form class="flex flex-wrap gap-2"><input class="form-input !min-h-10 w-56" name="q" value="{{ request('q') }}" placeholder="Cari transaksi atau nasabah"><select class="form-input !min-h-10 w-48" name="status"><option value="">Semua status</option>@foreach(['draft'=>'Draft','menunggu_verifikasi'=>'Menunggu Verifikasi','disetujui'=>'Disetujui','dikembalikan'=>'Dikembalikan','ditolak'=>'Ditolak','dibatalkan'=>'Dibatalkan','dikoreksi'=>'Dikoreksi'] as $value=>$label)<option value="{{ $value }}" @selected(request('status')===$value)>{{ $label }}</option>@endforeach</select><button class="soft-button">Cari</button></form></div>
<div class="table-shell"><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs font-bold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">No. Transaksi</th><th class="px-5 py-3">Berkas / Nasabah</th><th class="px-5 py-3">Kategori</th><th class="px-5 py-3">Nominal</th><th class="px-5 py-3">Status</th><th class="px-5 py-3">Tanggal</th><th class="px-5 py-3 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($transactions as $transaction)<tr class="hover:bg-blue-50/40"><td class="px-5 py-4 font-mono text-xs font-bold text-brand-700">{{ $transaction->transaction_number }}</td><td class="px-5 py-4"><p class="font-mono text-xs text-slate-500">{{ $transaction->serviceCase->file_number }}</p><p class="mt-1 font-semibold">{{ $transaction->customer->name }}</p></td><td class="px-5 py-4">{{ $transaction->category }}</td><td class="px-5 py-4 font-bold">Rp{{ number_format($transaction->amount,0,',','.') }}</td><td class="px-5 py-4">@include('partials.transaction-badge',['transaction'=>$transaction])</td><td class="px-5 py-4 text-xs text-slate-500">{{ $transaction->created_at->format('d/m/Y H:i') }}</td><td class="px-5 py-4 text-right"><a class="soft-button !py-2 !text-xs" href="{{ route('transactions.show',$transaction) }}">Detail</a></td></tr>@empty<tr><td colspan="7" class="px-5 py-12 text-center text-slate-500">Belum ada transaksi yang sesuai.</td></tr>@endforelse</tbody></table></div></div><div class="mt-5">{{ $transactions->links() }}</div>
@endsection
